# Privex Redis config - in-memory only, no disk persistence.
# save ""        → no RDB snapshots
# appendonly no  → no AOF
# Nothing user-related ever touches disk.

save ""
appendonly no
maxmemory-policy allkeys-lru
