apiVersion: v1
kind: Namespace
metadata:
  name: privex
  labels:
    app.kubernetes.io/part-of: privex
    # Default-deny ingress is enforced by networkpolicy.yaml; scrape access to
    # /metrics is granted only to the monitoring namespace.
    pod-security.kubernetes.io/enforce: restricted
