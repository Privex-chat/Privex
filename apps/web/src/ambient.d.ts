// Nym SDK is an OPTIONAL runtime dependency, loaded via a dynamic import only
// when present. Declare it so the type-checker doesn't require it to be installed.
declare module "@nymproject/sdk-full-fat";
