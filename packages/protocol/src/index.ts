// @privex/protocol - generated Protobuf types live in ./proto (built by
// protobufjs-cli from proto/*.proto). Import the `privex` namespace for all
// message types: e.g. `import { proto } from "@privex/protocol"` →
// `proto.privex.TextMessage`.
export * as proto from "./proto.js";

export const PROTOCOL_VERSION = 1;
